#!/usr/bin/env ruby

trap("INT") { exit 1 }


begin
  require_relative '../lib/schleuder/cli'
  Schleuder::Cli.start
rescue => exc
  $stderr.puts "Technical Error: #{exc}\n#{exc.backtrace.first}"
  exit 1
end
